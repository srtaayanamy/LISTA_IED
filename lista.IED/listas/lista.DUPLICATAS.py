# Implemente uma função que remova todos os elementos duplicados de uma lista.

lista = [49, 1, 2, 3, 4, 2, 1, 3]
def duplicatas (lista):
    sem_duplicatas = set(lista)
print(list(set(lista)))

